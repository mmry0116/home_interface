/**
 * @param {Array<string>} lines
 * @param {string} parentUrl
 * @returns {MasterPlaylist}
 */
export function parseMasterPlaylist(lines: Array<string>, parentUrl: string): MasterPlaylist;
import { MasterPlaylist } from "./model";
